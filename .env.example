# FarmIQ Environment Variables
# DO NOT commit your actual API key. Enter it via the Settings panel in the app.
# The app reads the key from localStorage (farmiq_gemini_key) at runtime.
VITE_GEMINI_API_KEY=your_gemini_api_key_here
